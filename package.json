{
  "name": "claude-versatile-root",
  "version": "0.3.0-alpha.0",
  "private": true,
  "description": "Claude Code orchestrator with external AI model delegation via MCP",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/lib && npm run build --workspaces --if-present",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Caishangqi/EnigmaClaudeVersatile"
  },
  "license": "MIT"
}
